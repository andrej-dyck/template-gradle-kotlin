dependencies {
    compile project(':domain-module')

    /* for contract tests */
    // testCompile project(':domain-module').sourceSets.test.output

    compile dependency.log4jApi
    runtime dependency.log4jCore
}

// alternative: apply from: "$rootDir/gradle/log4j.gradle"